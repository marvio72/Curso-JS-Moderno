// While se repite mientras que la opcion se verdadera

let i = 0;  // Inicializar el While

while (i < 10) { //condición
    console.log(i % 2 === 0 ? `${i} es par` : `${i} es impar`);
    i++; // Incremento
}