const autenticado = false;

if(autenticado) {
    console.log('Si puedes ver netflix');
} else {
    console.log('No pueder ver netflix');
}

// Operador ternario

console.log(autenticado ? 'Si puedes ver netflix' : 'No puedes ver netflix') ;